{% extends 'base.html.twig' %}

{% block title %}Log in  Wanderlust{% endblock %}

{% block content %}
<div class="min-h-[calc(100vh-64px)] bg-background flex relative overflow-hidden">
    <!-- Background Gradient and Globe (Inspired by explorer-sphere) -->
    <div class="hidden lg:flex flex-1 bg-gradient-to-br from-primary via-primary/80 to-accent items-center justify-center p-12 relative overflow-hidden">
        <div class="absolute inset-0 opacity-10">
            <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
                <path d="M0 100 C 20 0 50 0 100 100 Z" fill="white"></path>
            </svg>
        </div>
        <div class="relative z-10 text-center">
            <div class="w-20 h-20 bg-white/20 backdrop-blur-xl rounded-full flex items-center justify-center mx-auto mb-8 border border-white/30">
                <svg class="w-10 h-10 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="2" y1="12" x2="22" y2="12"></line>
                    <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
                </svg>
            </div>
            <h1 class="text-4xl font-heading font-bold text-white mb-4">Welcome Back</h1>
            <p class="text-white/80 text-lg max-w-md mx-auto font-medium">Continue your journey with Wanderlust. Discover more, experience more.</p>
        </div>
    </div>

    <!-- Login Form -->
    <div class="flex-1 flex items-center justify-center p-6 bg-background">
        <div class="w-full max-w-md">
            <div class="flex items-center gap-2 mb-8 lg:hidden">
                <svg class="w-8 h-8 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="2" y1="12" x2="22" y2="12"></line>
                    <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
                </svg>
                <span class="text-2xl font-heading font-bold text-foreground tracking-tight">Wanderlust</span>
            </div>

            <h2 class="text-3xl font-heading font-bold text-foreground mb-2">Sign in</h2>
            <p class="text-muted-foreground mb-8 font-medium">Enter your credentials to continue your adventure.</p>

            <form method="post" class="space-y-5">
                {% if error %}
                    <div class="bg-destructive/10 border border-destructive/20 text-destructive px-4 py-3 rounded-xl text-sm font-medium flex items-center gap-3">
                        <svg class="w-5 h-5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                <div class="space-y-2">
                    <label for="inputEmail" class="text-sm font-bold text-muted-foreground uppercase tracking-wider">Email Address</label>
                    <div class="relative group">
                        <div class="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground group-focus-within:text-primary transition-colors">
                            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
                        </div>
                        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="w-full bg-secondary/30 border border-border rounded-xl pl-12 pr-4 py-3 text-foreground font-medium focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all" placeholder="you@example.com" autocomplete="email" required autofocus>
                    </div>
                </div>

                <div class="space-y-2">
                    <div class="flex items-center justify-between">
                        <label for="inputPassword" class="text-sm font-bold text-muted-foreground uppercase tracking-wider">Password</label>
                            <a href="{{ path('app_forgot_password_request') }}" class="text-xs font-bold text-primary hover:underline transition-all">Forgot Password?</a>
                    </div>
                    <div class="relative group">
                        <div class="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground group-focus-within:text-primary transition-colors">
                            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg>
                        </div>
                        <input type="password" name="password" id="inputPassword" class="w-full bg-secondary/30 border border-border rounded-xl pl-12 pr-12 py-3 text-foreground font-medium focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all" placeholder="" autocomplete="current-password" required>
                        <button type="button" onclick="togglePassword('inputPassword', this)" class="absolute right-4 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-primary transition-colors focus:outline-none">
                            <svg class="w-5 h-5 eye-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>
                            <svg class="w-5 h-5 eye-off-icon hidden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>
                        </button>
                    </div>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <div class="flex items-center gap-2 py-2">
                    <input type="checkbox" id="remember_me" name="_remember_me" class="w-4 h-4 rounded border-border text-primary focus:ring-primary/20">
                    <label for="remember_me" class="text-sm font-medium text-muted-foreground">Keep me signed in</label>
                </div>

                <button class="w-full bg-primary hover:bg-primary/90 text-white font-bold py-3.5 rounded-xl transition-all shadow-sm hover:shadow-md flex items-center justify-center gap-2 text-lg" type="submit">
                    Sign in
                </button>

                <button id="faceLoginOpen" type="button" class="w-full bg-background border border-border hover:bg-secondary/50 text-foreground font-bold py-3.5 rounded-xl transition-all flex items-center justify-center gap-2">
                    <svg class="w-5 h-5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7V5a2 2 0 0 1 2-2h2"></path><path d="M17 3h2a2 2 0 0 1 2 2v2"></path><path d="M21 17v2a2 2 0 0 1-2 2h-2"></path><path d="M7 21H5a2 2 0 0 1-2-2v-2"></path><path d="M8 14s1.5 2 4 2 4-2 4-2"></path><path d="M9 9h.01"></path><path d="M15 9h.01"></path></svg>
                    FaceID Login
                </button>
            </form>

            <div id="faceLoginModal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/70 p-4">
                <div class="w-full max-w-lg rounded-2xl border border-border bg-background p-5 shadow-2xl">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-heading font-bold text-foreground">FaceID Login</h3>
                        <button id="faceLoginClose" type="button" class="text-muted-foreground hover:text-foreground text-sm font-semibold">Close</button>
                    </div>

                    <p class="text-sm text-muted-foreground mb-3">Center your face, capture a clear photo, then verify.</p>

                    <div id="faceCameraWrap" class="rounded-xl overflow-hidden bg-secondary/30 border border-border">
                        <video id="faceLoginVideo" autoplay playsinline class="w-full h-72 object-cover"></video>
                        <img id="faceLoginPreview" alt="Captured face" class="hidden w-full h-72 object-cover" />
                    </div>

                    <div id="faceLoginError" class="hidden mt-3 rounded-lg border border-destructive/30 bg-destructive/10 px-3 py-2 text-sm text-destructive"></div>

                    <div class="grid grid-cols-2 gap-3 mt-4">
                        <button id="faceLoginCapture" type="button" class="rounded-xl bg-primary text-white font-semibold py-2.5">Capture</button>
                        <button id="faceLoginRetake" type="button" class="rounded-xl border border-border font-semibold py-2.5 hover:bg-secondary/50 hidden">Retake</button>
                        <button id="faceLoginSubmit" type="button" class="col-span-2 rounded-xl bg-accent text-white font-semibold py-2.5 disabled:opacity-50 disabled:cursor-not-allowed" disabled>Verify and Login</button>
                    </div>
                </div>
            </div>

            <p class="text-center text-sm text-muted-foreground mt-10 font-medium">
                Don't have an account? <a href="{{ path('app_signup') }}" class="text-primary font-bold hover:underline transition-all">Create an account</a>
            </p>
        </div>
    </div>
</div>

<script>
    function togglePassword(inputId, btn) {
        const input = document.getElementById(inputId);
        const eyeIcon = btn.querySelector('.eye-icon');
        const eyeOffIcon = btn.querySelector('.eye-off-icon');

        if (input.type === 'password') {
            input.type = 'text';
            eyeIcon.classList.add('hidden');
            eyeOffIcon.classList.remove('hidden');
        } else {
            input.type = 'password';
            eyeIcon.classList.remove('hidden');
            eyeOffIcon.classList.add('hidden');
        }
    }

    (function () {
        const openBtn = document.getElementById('faceLoginOpen');
        const modal = document.getElementById('faceLoginModal');
        const closeBtn = document.getElementById('faceLoginClose');
        const captureBtn = document.getElementById('faceLoginCapture');
        const retakeBtn = document.getElementById('faceLoginRetake');
        const submitBtn = document.getElementById('faceLoginSubmit');
        const errorBox = document.getElementById('faceLoginError');
        const video = document.getElementById('faceLoginVideo');
        const preview = document.getElementById('faceLoginPreview');
        const endpoint = '{{ path('app_login_face_id') }}';
        const csrfToken = '{{ csrf_token('face_login') }}';

        let stream = null;
        let capturedFaceData = '';

        function showError(message) {
            errorBox.textContent = message;
            errorBox.classList.remove('hidden');
        }

        function clearError() {
            errorBox.classList.add('hidden');
            errorBox.textContent = '';
        }

        async function startCamera() {
            clearError();
            capturedFaceData = '';
            submitBtn.disabled = true;
            retakeBtn.classList.add('hidden');
            preview.classList.add('hidden');
            video.classList.remove('hidden');

            try {
                stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: 'user' }, audio: false });
                video.srcObject = stream;
            } catch (e) {
                showError('Cannot access camera. Allow camera permission and try again.');
            }
        }

        function stopCamera() {
            if (!stream) {
                return;
            }

            for (const track of stream.getTracks()) {
                track.stop();
            }
            stream = null;
            video.srcObject = null;
        }

        function openModal() {
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            startCamera();
        }

        function closeModal() {
            stopCamera();
            modal.classList.add('hidden');
            modal.classList.remove('flex');
        }

        function captureFace() {
            clearError();
            if (!video.videoWidth || !video.videoHeight) {
                showError('Camera is not ready yet. Please wait one second and retry.');
                return;
            }

            const canvas = document.createElement('canvas');
            canvas.width = video.videoWidth;
            canvas.height = video.videoHeight;
            const ctx = canvas.getContext('2d');
            if (!ctx) {
                showError('Could not capture frame from camera.');
                return;
            }

            ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
            capturedFaceData = canvas.toDataURL('image/jpeg', 0.95);
            preview.src = capturedFaceData;
            preview.classList.remove('hidden');
            video.classList.add('hidden');
            retakeBtn.classList.remove('hidden');
            submitBtn.disabled = false;
            stopCamera();
        }

        async function submitFaceLogin() {
            clearError();
            if (!capturedFaceData) {
                showError('Capture your face before verification.');
                return;
            }

            submitBtn.disabled = true;
            const previousText = submitBtn.textContent;
            submitBtn.textContent = 'Verifying...';

            const formData = new FormData();
            formData.append('faceImageData', capturedFaceData);
            formData.append('_token', csrfToken);

            try {
                const response = await fetch(endpoint, {
                    method: 'POST',
                    body: formData,
                    credentials: 'same-origin',
                });
                const payload = await response.json();

                if (!response.ok || !payload.success) {
                    throw new Error(payload.message || 'Face login failed.');
                }

                window.location.href = payload.redirectUrl || '{{ path('app_home') }}';
            } catch (e) {
                showError(e.message || 'Face login failed. Please try again.');
                submitBtn.disabled = false;
                submitBtn.textContent = previousText;
            }
        }

        openBtn.addEventListener('click', openModal);
        closeBtn.addEventListener('click', closeModal);
        captureBtn.addEventListener('click', captureFace);
        retakeBtn.addEventListener('click', startCamera);
        submitBtn.addEventListener('click', submitFaceLogin);
        modal.addEventListener('click', function (event) {
            if (event.target === modal) {
                closeModal();
            }
        });
    })();
</script>
{% endblock %}
